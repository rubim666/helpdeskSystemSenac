<?xml version="1.0" encoding="utf-8"?>
<!-- Allows cleartext HTTP only to the local dev backend IP; keep HTTPS-only for all other domains -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.62.54.36</domain>
    </domain-config>
</network-security-config>
